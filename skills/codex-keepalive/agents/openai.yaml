interface:
  display_name: "Codex Keepalive"
  short_description: "Keep active goals resumable"
  default_prompt: "Use $codex-keepalive to configure explicit GNU Screen resume automation for this goal."

policy:
  allow_implicit_invocation: false
