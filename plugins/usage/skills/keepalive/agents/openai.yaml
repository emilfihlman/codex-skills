interface:
  display_name: "Usage Keepalive"
  short_description: "Keep active goals resumable"
  default_prompt: "Use $usage:keepalive to configure explicit GNU Screen resume automation for this goal."

policy:
  allow_implicit_invocation: false
